import styled from 'styled-components'

export const HeaderContainer = styled.div`
  width: 100%;
  font-weight: 400;
  line-height: 145%;
  font-size: 1.25rem;
  color: #333333;

  ul {
    display: flex;
    gap: 1.43rem;

    li {
      list-style: none;
    }
    span {
      color: rgba(147, 149, 150, 0.65);
    }
  }
`
